# IssueFlow production application — Web P4.8.1

This is the single application source. See the repository README for setup and deployment.

Run npm.cmd ci, npm.cmd run lint, npm.cmd test and npm.cmd run build from this folder.

Use manifests/issueflow-production-dev-p4-8.xml after deployment. The stable manifest filename and add-in ID are unchanged; its version is 1.0.0.9.
See [current release and acceptance checks](docs/P4_8_1_WEB_RELEASE.md) and [web security guidance](docs/SECURITY.md).
