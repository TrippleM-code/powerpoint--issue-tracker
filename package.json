{
  "name": "issueflow-repository",
  "private": true,
  "version": "1.0.0-p4.8",
  "type": "module",
  "scripts": {
    "setup": "npm ci --prefix production",
    "dev": "npm run dev --prefix production",
    "build": "npm run build --prefix production",
    "test": "npm test --prefix production",
    "test:watch": "npm run test:watch --prefix production",
    "lint": "npm run lint --prefix production"
  }
}
